import {useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {enquiriesApi} from '../api/apis'; const categories=['CARD_PAYMENT_DISPUTE','CARD_FRAUD','CARD_SERVICES','ATM_QUERY','ATM_DISPUTE','BANK_TRANSFER','ACCOUNT_QUERY','IDENTITY_VERIFICATION','FEES_AND_CHARGES','LOAN_QUERY','MORTGAGE_QUERY','GENERAL_BANKING'];const languages=['ENGLISH','MANDARIN','HINDI','MALAY','TAMIL'];
export function CustomerPage(){const nav=useNavigate();const [form,setForm]=useState({customerId:'CUST-',category:'CARD_FRAUD',preferredLanguage:'ENGLISH',message:''}),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);try{const enquiry=await enquiriesApi.create(form);nav(`/enquiries/${enquiry.enquiryId}`)}catch(err){setError((err as Error).message)}finally{setBusy(false)}}return <section className="form-page"><h1>Customer Enquiry</h1><p>Create an enquiry and connect to the first suitable available agent.</p><form onSubmit={submit}><label>Customer ID<input required value={form.customerId} onChange={e=>setForm({...form,customerId:e.target.value})}/></label><label>Enquiry Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}>{categories.map(x=><option key={x}>{x}</option>)}</select></label><label>Preferred Language<select value={form.preferredLanguage} onChange={e=>setForm({...form,preferredLanguage:e.target.value})}>{languages.map(x=><option key={x}>{x}</option>)}</select></label><label>Message<textarea required value={form.message} onChange={e=>setForm({...form,message:e.target.value})} placeholder="How can we help?"/></label>{error&&<p className="error">{error}</p>}<button disabled={busy}>{busy?'Creating…':'Create Enquiry'}</button></form></section>}
